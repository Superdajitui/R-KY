# 部署指南 —— 让任何人都能访问

目标：拿到一个固定网址，**任何设备、任何网络**都能打开，不需要你的电脑开着。

用的是 **GitHub Pages**：免费、永久、自带 HTTPS，不用买服务器也不用备案。

整个过程大约 15 分钟，只需要做一次。

---

## 第 1 步：注册 GitHub 账号

打开 → **https://github.com/signup**

依次填：

| 项目 | 说明 |
|---|---|
| Email | 你的邮箱，**必须能收信**（要验证） |
| Password | 至少 15 位，或至少 8 位且含数字和小写字母 |
| Username | **这个很重要**，它会出现在你的网址里 |
| 邮箱验证码 | GitHub 会发一封邮件，把 6 位数字填进去 |

> **用户名怎么选？**
> 你的网址会是 `https://用户名.github.io/R-KY/`，所以用户名越短越好。
> 你之前定的 `R-KY` 如果被占用，就换一个（比如 `kerry-ren`、`rky-ren`、
> 加上数字等）。**注册完把用户名记下来**，下一步要用 ——
> 不用担心，部署脚本会问你，写错了重跑一次就行。

> 如果注册页面卡住或加载不出来，多半是网络问题，换个时间或换个网络再试。

---

## 第 2 步：创建一个空仓库

打开 → **https://github.com/new**

| 字段 | 填什么 |
|---|---|
| Repository name | `R-KY` ← **必须一模一样，区分大小写** |
| Description | 随便填，比如 `personal website` |
| Public / Private | 选 **Public** |
| Add a README file | **不要勾选** |
| .gitignore / license | **都不要选** |

点绿色按钮 **Create repository**。

> 仓库名必须是 `R-KY`，因为网站里的路径已经按这个名字配好了。
> 如果你非要用别的名字，告诉我，我改配置。

> **为什么必须 Public？** GitHub Pages 的免费版只支持公开仓库。
> 网站内容本来就是给人看的，公开没有影响。

---

## 第 3 步：一键推送

回到项目文件夹，**双击 `deploy-github.bat`**。

脚本会：

1. 问你的 GitHub 用户名 → 输入第 1 步注册的那个
2. 打包生产文件到 `docs/`
3. 初始化 git 并提交
4. 推送到 GitHub

> **第一次推送会弹出浏览器让你登录 GitHub**，点授权就行。
> 这是 Git Credential Manager 在帮你登录，不需要手动配 SSH key 或 token，
> 登录一次以后就不用再登了。

看到 `推送成功！` 就说明代码上去了。

---

## 第 4 步：开启 GitHub Pages（最后一步）

**这一步必须在网页上点，脚本代替不了。**

打开 → `https://github.com/你的用户名/R-KY/settings/pages`

按下表设置：

| 字段 | 选什么 |
|---|---|
| Source | **Deploy from a branch** |
| Branch | **main** |
| 右边的文件夹 | **/docs** ← 别选成 /(root) |
| 然后点 | **Save** |

等 **1~2 分钟**，刷新这个页面，顶部会出现绿色提示：

```
Your site is live at https://你的用户名.github.io/R-KY/
```

**这个地址就是你的网站**。手机、平板、别人的电脑，随便什么网络都能打开。

---

## 之后怎么更新网站

改完内容后，**再双击一次 `deploy-github.bat`** 即可，两步都不用重做。
它会自动重新打包、提交、推送，GitHub Pages 会在一两分钟内自动更新。

如果你想在本地先看看效果，双击 `打开网站.bat`。

---

## 常见问题

**Q：推送失败，说 repository not found**

第 2 步的仓库没建，或者名字不是 `R-KY`。
去 https://github.com/new 建一个，然后重跑 `deploy-github.bat`。

**Q：网页打开是 404**

- 第 4 步的文件夹选错了 —— 必须是 **/docs**，不是 /(root)
- 或者刚点完 Save，还没构建完，等两分钟再刷新
- 确认仓库里有 `docs` 文件夹：`https://github.com/你的用户名/R-KY`

**Q：页面能打开但样式全没了 / 照片不显示**

这种情况几乎都是路径问题。本地跑一遍子路径测试就能查出来：

```bash
node tools/test-subpath.mjs
```

**Q：国内访问 github.io 很慢或者打不开**

GitHub Pages 在国内时快时慢，这是网络环境问题，不是网站的问题。三个选择：

1. **先凑合用** —— 换网络（手机热点）经常就好了
2. **绑定自己的域名** —— 买个域名（几十块一年）绑到 GitHub Pages，
   再套一层国内能访问的 CDN。告诉我，我帮你配
3. **换国内云** —— 阿里云 / 腾讯云的对象存储，国内访问最快，
   但需要付费（几块钱一个月），自定义域名还要备案

**Q：以后想换个网址怎么办**

换个用户名或仓库名，然后：
1. 改 `tools/deploy.ps1` 里的 `$REPO`
2. 重跑 `deploy-github.bat`
3. 在 GitHub 上重新开一次 Pages

**Q：网站会被搜索引擎收录吗**

会。`robots.txt` 和 `sitemap.xml` 已经生成好了。
如果你暂时不想被搜到，告诉我，我把 `robots.txt` 改成禁止抓取。

---

## 技术说明（供参考）

- **托管**：GitHub Pages，免费，自动 HTTPS
- **发布目录**：`docs/`（`main` 分支）
- **打包**：`node tools/build.mjs https://用户名.github.io/R-KY`
- **产物**：19 个文件，约 693 KB，含照片和字体，无任何外部依赖
- **无构建步骤**：纯静态 HTML/CSS/JS，不用打包器也不用框架

打包脚本会自动做三件容易出错的事：

1. 把 `og:image`、`canonical` 补成**绝对地址**（微信、Twitter 抓取器只认绝对地址）
2. 生成 `.nojekyll`，阻止 GitHub 用 Jekyll 处理静态文件
3. **扫描产物里有没有 `/assets/...` 这种指向根目录的绝对路径** ——
   项目站点跑在 `/R-KY/` 子路径下，这种写法必然 404，
   而它在本地根路径预览时完全看不出来
